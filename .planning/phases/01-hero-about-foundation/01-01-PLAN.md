---
phase: 01-hero-about-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/portfolio/HeroSection.tsx
autonomous: true
requirements:
  - PORT-01
must_haves:
  truths:
    - "User immediately sees role, short value statement, and primary CTA in Hero first view on mobile and desktop."
    - "Primary CTA from Hero leads user toward project browsing flow without dead-end interaction."
    - "Hero animation/decorative behavior does not block readability, especially for reduced-motion users."
  artifacts:
    - path: "frontend/src/components/layout/portfolio/HeroSection.tsx"
      provides: "Hero semantic content, CTA behavior, and motion-safe client interactions."
      min_lines: 120
  key_links:
    - from: "frontend/src/components/layout/portfolio/HeroSection.tsx"
      to: "#projects"
      via: "primary CTA anchor/action"
      pattern: "href=\"#projects\"|window\\.location\\.hash = \"#projects\""
    - from: "frontend/src/components/layout/portfolio/HeroSection.tsx"
      to: "prefers-reduced-motion"
      via: "conditional animation degradation"
      pattern: "prefers-reduced-motion|matchMedia"
---

<objective>
Deliver a Hero section that communicates identity/value in seconds and provides a clear first action aligned with recruiter browsing flow.

Purpose: Satisfy PORT-01 with production-ready, readable Hero behavior consistent with the approved UI contract.
Output: Updated Hero implementation with clear role/value copy, explicit primary CTA behavior, and reduced-motion-safe interaction.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-hero-about-foundation/01-UI-SPEC.md
@frontend/src/components/layout/portfolio/HeroSection.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Align hero content and CTA to UI contract</name>
  <read_first>
    - .planning/phases/01-hero-about-foundation/01-UI-SPEC.md
    - frontend/src/components/layout/portfolio/HeroSection.tsx
  </read_first>
  <files>frontend/src/components/layout/portfolio/HeroSection.tsx</files>
  <acceptance_criteria>
    - Hero first view includes role headline, short value statement, and exactly one dominant primary CTA label aligned with copy contract intent.
    - Primary CTA performs concrete navigation to `#projects` (anchor or equivalent deterministic behavior), not placeholder button interaction.
    - Hero markup keeps semantic heading/paragraph/button-or-anchor structure and does not introduce inline CSS.
  </acceptance_criteria>
  <action>Refactor Hero content block per UI-SPEC interaction and copy constraints: keep role/value messaging scannable on first viewport, convert primary CTA into deterministic navigation target (`#projects`), and remove placeholder/debug interaction patterns (including console debug traces) that are not production-safe.</action>
  <verify>
    <automated>cd frontend && yarn lint --file src/components/layout/portfolio/HeroSection.tsx</automated>
  </verify>
  <done>Hero first-view message and CTA are production-ready, deterministic, and directly satisfy PORT-01 without temporary placeholders.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Enforce reduced-motion-safe hero interaction logic</name>
  <read_first>
    - .planning/phases/01-hero-about-foundation/01-UI-SPEC.md
    - frontend/src/components/layout/portfolio/HeroSection.tsx
  </read_first>
  <files>frontend/src/components/layout/portfolio/HeroSection.tsx</files>
  <acceptance_criteria>
    - When `prefers-reduced-motion: reduce` is active, long/pulse/parallax-like animation behaviors are disabled or degraded while content remains fully visible.
    - Default-motion path still reveals hero content without hidden elements remaining invisible after initial render.
    - Motion logic remains encapsulated in component behavior and does not require new global runtime dependencies.
  </acceptance_criteria>
  <action>Update Hero GSAP initialization flow to include explicit reduced-motion guard path: skip non-essential timeline effects, force visible final states for key hero elements, and preserve normal animation only for non-reduced-motion contexts to keep readability and accessibility baseline from UI-SPEC.</action>
  <verify>
    <automated>cd frontend && yarn lint --file src/components/layout/portfolio/HeroSection.tsx</automated>
  </verify>
  <done>Hero interaction honors reduced-motion preference and never compromises immediate readability/visibility of primary content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser preference -> animation runtime | Untrusted runtime/browser settings (reduced motion, low-power behavior) influence client animation path |
| UI action -> in-page navigation | User-triggered CTA should only move within trusted in-page anchors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | D (Denial of Service) | `HeroSection` animation timeline | mitigate | Add reduced-motion guard and explicit visible end-state so animation path cannot block content rendering on constrained devices |
| T-01-02 | T (Tampering) | Hero CTA interaction | mitigate | Keep CTA destination hardcoded to trusted in-page anchor `#projects`; do not read navigation target from user-controlled input |
| T-01-03 | I (Information Disclosure) | Debug logs in hero runtime | mitigate | Remove development console traces from production hero flow to avoid leaking implementation details and noisy runtime behavior |
</threat_model>

<verification>
Run lint for modified Hero file and validate first-view behavior in local dev:
- `cd frontend && yarn lint --file src/components/layout/portfolio/HeroSection.tsx`
- `cd frontend && yarn dev` then verify Hero first viewport and CTA navigation.
</verification>

<success_criteria>
- Hero immediately communicates role + value + primary CTA on first view (PORT-01).
- CTA has deterministic navigation and no dead button state.
- Reduced-motion preference keeps Hero fully usable without readability regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hero-about-foundation/01-01-SUMMARY.md`
</output>
